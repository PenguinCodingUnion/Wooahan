export enum GameStatus {
  GAME_NOT_LOADED,
  GAME_READY,
  GAME_START,
  GAME_PAUSE,
  GAME_END,
}

export enum PengulAnimation {
  T_POSE,
  IDLE,
  JUMPING,
  WALK,
  SURPRIZE,
  SWIM,
  WAVING,
  JUMP_JUMP,
  VICTORY,
}